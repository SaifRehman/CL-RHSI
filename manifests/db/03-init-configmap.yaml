apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: demo-db
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS todos (
        id SERIAL PRIMARY KEY,
        title TEXT NOT NULL,
        completed BOOLEAN NOT NULL DEFAULT FALSE
    );
